#!/bin/bash

ufw allow OpenSSH
echo "y" | ufw enable
ufw status

# Output the environment vars in the parameters file.
if [[ -e /root/stablediff-script-params.sh ]]; then
    source /root/stablediff-script-params.sh
fi


	echo
	echo "Finished!"
	echo
