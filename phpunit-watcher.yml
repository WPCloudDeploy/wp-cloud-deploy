watch:
    directories:
        - includes/core
        - tests/phpunit
    fileMask: '*.php'
notifications:
    passingTests: true
    failingTests: true
phpunit:
    binaryPath: vendor/bin/phpunit
    arguments: '--stop-on-failure'
    timeout: 180
